pragma circom 2.1.0;

// Select between two values based on a boolean selector s∈{0,1}.
template Switch() {
    signal input in[2];
    signal input s;
    signal output out[2];

    s * (1 - s) === 0;
    out[0] <== (in[1] - in[0]) * s + in[0];
    out[1] <== (in[0] - in[1]) * s + in[1];
}

